# Premium And Product-Critical Loop Plan

## Governing Scenario

Run two separated benchmark/correction sprints on the remaining high-value
visual/runtime surfaces that still matter after the runtime-sensitive loop:

1. `premium interface discipline`
2. `product-critical surface discipline` under stronger visual/compositional
   pressure

Each sprint should:

1. run the local/legacy comparison at `medium` and `high`
2. identify any real local deficit
3. patch the local doctrine only if the deficit is real
4. rerun the affected side
5. issue or update the parity decision

## Selected Workflow Adapter Or Current No-Adapter Reality

Current pre-agents no-adapter reality. This plan is the governing artifact for
the loop.

## Selected Stack Profile

- standalone `accelerate`
- benchmark-driven parity / replacement gate
- visual/high-value surface review

## Selected Runtime Adapters

- none

## Selected Docs Posture

- executive parity packets
- scoreboard updates
- narrow doctrine hardenings only when benchmark evidence justifies them
- `SKILL.md` cleanup after benchmark closure

## Recommended Adjacent Skills

- `accelerate`
- `verification-before-completion`

## Recommended Future Agent Candidates

- none; this loop is tightly coupled benchmark orchestration plus root-skill
  cleanup

## Explicit Non-Goals

- no new benchmark family outside these two sprints
- no speculative doctrine growth without benchmark evidence
- no `SKILL.md` rewrite before the premium/product-critical evidence is in hand

## Next Bounded Setup Or Implementation Slices

### Sprint 1: Premium Interface Discipline

- benchmark `local` vs `legacy` at `medium` and `high`
- focus on:
  - premium billing recovery / upgrade / monetization-type surface
  - reference-to-wireframe protocol
  - `Reference ASCII` / `Target ASCII`
  - hierarchy and CTA dominance
  - premium failure modes:
    - `card-soup`
    - `cta-dilution`
    - `flat-hierarchy`
    - `reference-as-moodboard`

### Sprint 2: Product-Critical Visual Pressure

- benchmark `local` vs `legacy` at `medium` and `high`
- focus on:
  - product-critical surface with strong composition pressure but without
    depending only on premium-language framing
  - backend product contract sufficiency plus visual-contract sufficiency
  - isolation / dominant action / no-close principle under stronger layout drift

### Sprint 3: Root `SKILL.md` Cleanup

- inspect `SKILL.md` against:
  - repo `AGENTS.md`
  - local native authorities
  - global skill overlap
- remove duplication
- tighten reading order and logical chain
- preserve root compactness while keeping the reasoning ladder explicit

### Registration

- issue per-surface parity packets when evidence is strong enough
- update the gap matrix and executive index
- summarize the global result after both sprints and the `SKILL.md` cleanup

## Residual Risks Or Open Decisions

- the premium benchmark may need a clearer visual-reference contract than a
  purely textual scenario; if so, the benchmark should still make the missing
  reference-handling obligations explicit
- some legacy reruns may consult local-native repo surfaces and lower
  comparison cleanliness; if that happens, record the caveat explicitly
