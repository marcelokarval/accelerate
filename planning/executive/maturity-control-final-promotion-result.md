# Maturity Control Final Promotion Result

## Purpose

Register the final bounded promotion arbitration for `maturity control`.

This follows:

- `maturity-control-post-subagent-rerun-result.md`
- `maturity-control-loop-stop-agent-factory-transition-result.md`

## Runtime Posture

- date: 2026-04-21
- repository phase: standalone pre-agents
- workflow backend: pre-adapter / no-backend
- issue stack posture: approved no-backend executive-artifact exception
- proof lane: read-only benchmark judges plus durable executive registration
- model: `gpt-5.4`
- effort: `high`
- local judge output: `/tmp/accelerate-maturity-final-local.out`
- legacy/global judge output: `/tmp/accelerate-maturity-final-legacy.out`

## Evidence Under Arbitration

The promotion decision used two direct maturity-control behavior cycles.

### Cycle 1: Post-Subagent Rerun

The local run:

- stopped maturity-control promotion pressure
- redirected to `subagent model`
- corrected the smallest owning surface
- reran focused local and legacy/global judges
- promoted only bounded delegation governance
- avoided maturity-control doctrine expansion

### Cycle 2: Loop Stop And Agent-Factory Transition

The local run:

- stopped another low-value maturity-promotion loop
- moved to native `agent-factory`
- rehomed `agents/doctrine/empirical-replay.md`
- avoided new maturity-control doctrine
- kept future agents, provider-boundary ownership, and runtime agent claims out
  of scope

## Judge Results

| Judge | Verdict | Reason |
| --- | --- | --- |
| local-authority judge | `promote local at parity` | the prior blocker was repeated direct behavior; the two direct cycles now prove stopping, shrinking, redirecting, smallest-home routing, and bounded promotion discipline |
| legacy/global judge | `promote local at parity` | local now matches legacy maturity-control behavior in practice for the bounded documented scenario, while legacy keeps only broader historical runtime-confidence advantage |

## Root Arbitration

Decision:

- promote `maturity control` to bounded `local at parity`

Do not promote to:

- `local ahead`

## Bounded Promotion Scope

`maturity control` is now `local at parity` for standalone pre-agents governance
behavior that:

- stops low-value promotion loops
- shrinks or redirects process when enough proof exists
- routes durable learning to the smallest correct home
- blocks doctrine expansion when restraint is the mature answer
- preserves bounded promotion and exclusion discipline

## Explicit Exclusions

This promotion does not claim:

- broad runtime maturity
- workflow-adapter/backend maturity
- future `*.toml` agent readiness
- scheduled observers
- provider-boundary ownership
- `local ahead`
- live agent-factory runtime maturity

## Doctrine Decision

Do not mutate:

- `core/workflows/maturity-control.md`

The mature behavior was demonstrated through execution and registration, not
through another rule.

## AI Review Report

### Self-Review

The promotion is evidence-backed and bounded. It does not reward document
completeness alone.

### Self-Forensic Review

The main overclaim risk is confusing bounded pre-agents maturity-control parity
with live runtime maturity. This packet keeps those claims explicitly separate.
