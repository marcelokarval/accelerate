# Maturity Control Post Self-Evolution Rerun Result

## Purpose

This artifact records the promotion-targeted rerun for `maturity control` after
two adjacent repeatability surfaces were promoted:

- `parity / replacement gate`
- `autoresearch / self-evolution`

The question was whether those later promotions provide enough direct
behavioral evidence to move `maturity control` from `near parity` to
`local at parity`.

## Runtime Posture

- repository phase: standalone pre-agents
- workflow backend: pre-adapter / no-backend
- issue stack posture: approved no-backend executive-artifact exception
- proof lane: read-only benchmark judges plus durable executive registration
- model / effort:
  - `gpt-5.4`
  - `high`
- local judge output:
  - `/tmp/accelerate-maturity-local-promotion.out`
- legacy/global judge output:
  - `/tmp/accelerate-maturity-legacy-promotion.out`

## Scenario

Prior maturity-control judgment kept the surface at `near parity` because the
local file had method coverage but lacked repeated behavioral proof.

After that, the repo produced two adjacent signals:

- `parity / replacement gate` promoted to `local at parity`
- `autoresearch / self-evolution` promoted to `local at parity` for the bounded
  document-backed external workflow-learning loop

This rerun tested whether those adjacent signals are enough to promote
`maturity control` itself.

## Judge Results

| Judge | Verdict | Reason |
| --- | --- | --- |
| local standalone judge | `keep near parity` | later promotions prove their own surfaces, but do not yet prove direct maturity-control behavior across natural cycles |
| legacy/global judge | `keep near parity` | adjacent evidence is positive, but the legacy standard still requires direct proof of de-escalation, anti-bloat behavior, and smallest-home routing |

## Root Arbitration

Both judges converge.

Decision:

- `maturity control`: `near parity`

Do not promote to:

- `local at parity`
- `local ahead`

## Evidence Considered

### Positive Adjacent Evidence

The later cycles did show maturity-compatible behavior:

- `parity / replacement gate` refused to launder adjacent `near parity`
  surfaces into its own promotion.
- `self-evolution` promoted only after a second independent replay.
- `self-evolution` bounded its promotion to document-backed external workflow
  learning.
- `web-reader` replay rejected direct import, avoided immediate core mutation,
  and preserved only a future insertion candidate.

### Why That Is Not Enough

Those signals prove:

- `parity / replacement gate` behavior
- `self-evolution` behavior
- external skill vetting and skill-evaluation behavior

They do not yet directly prove that `maturity control` repeatedly:

- collapses unnecessary process when enough proof already exists
- stops low-value benchmark loops instead of recording another artifact
- routes durable learnings to the smallest correct home as a named governing
  reason
- prevents new doctrine when restraint is the mature answer

## Residual Gap

The remaining gap is behavioral, not textual.

The next qualifying evidence should come from a natural cycle where the correct
maturity-control answer is to stop, shrink, or relocate work rather than create
another promotion-targeted benchmark.

## Decision

Keep:

- `maturity control`: `near parity`

Do not mutate:

- `core/workflows/maturity-control.md`

Do not add:

- a new maturity-control rule
- a new benchmark-specific lane

## Next Operating Move

Move the active convergence focus away from maturity-control promotion.

The best next surface is:

- `subagent model`

Reason:

- it remains a real structural `near parity` surface
- it is future-agent critical
- it can be tested with delegation scenarios instead of another maturity-control
  meta-loop

## AI Review Report

### Self-Review

The result avoids converting positive adjacent behavior into an unsupported
surface promotion. It preserves the stricter parity ladder.

### Self-Forensic Review

Main overclaim risk:

- treating bounded self-evolution restraint as full maturity-control proof

Mitigation:

- verdict remains `near parity`
- next action moves away from maturity-control promotion pressure
