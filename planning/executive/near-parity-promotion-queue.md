# Executive Queue: Near-Parity Promotion

## Purpose

This artifact converts the current `near parity` batch into an execution queue.

It exists to answer:

- which surfaces should be promoted first
- which surfaces are mostly blocked by real reruns
- which surfaces can realistically move to `local at parity` earliest

This is not a new gap matrix.

It is the operational queue that follows the parity-decision batch.

## Promotion Rule

Promote the surfaces that are:

1. most likely to show behavioral evidence quickly
2. most likely to unblock other surfaces indirectly
3. least dependent on future-agent assumptions

Do not prioritize by document beauty or by which file is easiest to edit.

## Tier 1: Immediate Promotion Targets

### 1. Operational Calibration

Why first:

- it directly governs selection harshness
- it is the cleanest explanation for several residual behavioral misses
- it influences scope recovery, orchestration weight, and failure-mode naming

What would upgrade it:

- repeated real runs where the local chooses the correct proportional process
  without over-literal or over-broad recovery
- explicit proof that the local applies calibration sharply rather than only
  describing it well

Current target verdict:

- satisfied; promoted beyond `near parity`

Current evidence status:

- rerun after native hardening registered in
  `operational-calibration-parity-decision.md`
- current result is `local at parity`; the local now keeps calibration-only
  slices in the smallest valid bounded process

### 2. Issue Stack

Why second:

- it is central to mutating-work discipline
- it is easy to observe behaviorally
- success here strengthens closure truth across many other surfaces

What would upgrade it:

- repeated proof that the local blocks execution when issue bootstrap or
  post-issue planning gates are under-run
- repeated proof that issue hygiene is not retrofitted after implementation

Current target verdict:

- satisfied; promoted beyond `near parity`

Current evidence status:

- rerun after native hardening registered in `issue-stack-parity-decision.md`
- current result is `local ahead`; the local now keeps the benchmark slice in
  calibration mode while expressing shaping-first mutation ordering more
  cleanly than the legacy/reference-backed path

### 3. Branch Enforcement Matrix

Why third:

- it is the routing and blocking surface behind many execution decisions
- stronger branch enforcement should make later reruns less ambiguous

What would upgrade it:

- repeated live proof that inherited gates and validation failures are treated
  as hard blockers
- proof that branch-specific mandatory skills are loaded without retrospective
  correction

Current target verdict:

- satisfied; promoted beyond `near parity`

Current evidence status:

- first focused parity packet issued in
  `branch-enforcement-matrix-parity-decision.md`
- current result is `local ahead`; the local side matched the legacy on core
  blocking behavior and was more precise on the native cross-surface gate set

## Tier 2: High-Value Follow-On Targets

### 4. Current Enforcement Surfaces

Why here:

- it depends on the same behavioral sharpness as the branch matrix
- it becomes easier to prove once branch enforcement is behaving consistently

Target:

- satisfied; promoted beyond `near parity`

Current evidence status:

- first focused parity packet issued in
  `current-enforcement-surfaces-parity-decision.md`
- current result is `local ahead`; the local side blocked closure with equal
  harshness and named the harvested concrete violations more explicitly on a
  billing/self-service slice

### 5. QA / Proof Stack

Why here:

- it is central, but the proof is more runtime- and operator-dependent
- it should be upgraded after issue / branch discipline is behaving cleanly

Target:

- satisfied; promoted beyond `near parity`

Current evidence status:

- 360 rerun registered in `qa-proof-stack-parity-decision.md`
- current result is `local ahead`; the local side now names proof-order
  failures, lane-state blocking, and product-critical escalation more cleanly

### 6. Workflow-Change Approval Gate

Why here:

- it already exists and is wired correctly
- what it lacks is repeated real use, which becomes easier once more rerun
  cycles are executed under the queue

Target:

- satisfied; promoted beyond `near parity`

Current evidence status:

- focused approval-gate retest registered in
  `workflow-change-approval-gate-parity-decision.md`
- current result is `local at parity`; both local and reference-backed paths
  block workflow-doctrine mutation without explicit HITL approval for the exact
  change

### 7. Workflow Catalog

Why here:

- the latest focused catalog retest exposed a real under-call in the local
  runtime selection
- benchmark registration is now recurring governance work and deserves a named
  lane rather than composition by implication

Target:

- satisfied; promoted beyond `near parity`

Current evidence status:

- latest retest found that the legacy/reference-backed path more sharply
  required issue/bootstrap posture for living-doc registration and distinguished
  standard comparative benchmark evidence from lighter synthetic reruns
- native correction landed in `core/workflows/catalog.md` and
  `core/control-plane/quick-invocation-map.md`
- post-correction explicit and light local reruns showed the written local
  workflow set now selects the expected lane family and refuses premature
  promotion
- a full independent local-vs-legacy comparative rerun then aligned on the
  same lane family, blockers, evidence rules, and conservative no-promotion
  verdict
- a promotion-targeted rerun split between local conservatism and
  legacy/global promotion; root arbitration followed the parity gate ladder and
  promoted to `local at parity`
- the durable packet is
  `workflow-catalog-post-correction-rerun-result.md`
- the full comparative packet is
  `workflow-catalog-full-comparative-rerun-result.md`
- the promotion-targeted packet is
  `workflow-catalog-promotion-targeted-rerun-result.md`
- residual risk is now live-session monitoring, not a promotion blocker

## Tier 3: Maturity And Repeatability Targets

### 8. Autoresearch / Self-Evolution
### 9. Maturity Control

Why grouped:

- these are not mainly blocked by missing text
- they are blocked by repeated organizational use
- they should be promoted only after enough real rerun cycles exist

Target:

- `autoresearch / self-evolution`: satisfied; promoted beyond `near parity`
- `maturity control`: stay `near parity` until repetition evidence accumulates
  naturally

Current evidence status:

- focused durable-evidence rerun registered in
  `self-evolution-durable-evidence-rerun-result.md`
- both local and legacy/global judges kept `autoresearch / self-evolution` at
  `near parity`
- correction landed as a narrow durable-capture rule across self-evolution,
  operational calibration, and the parity replacement gate
- a post-correction local-authority rerun confirmed the new rule blocks
  transient-only closure, but did not justify promotion in the same cycle
- a second post-commit local-authority rerun confirmed the committed rule is
  correctly placed, but still rejected promotion because one surface-specific
  correction cycle is not repeated organizational proof
- natural follow-up rerun registered in
  `self-evolution-natural-followup-rerun-result.md`
- both local and legacy/global judges kept `autoresearch / self-evolution` at
  `near parity` after the parity-gate promotion cycle
- next validation should come from a real recurring failure or workflow-learning
  event, not another synthetic promotion question
- first real external workflow-learning event registered in
  `zai-skill-stack-harvest-analysis.md`
- Z.ai / GLM skill-stack harvest identified concrete additions for external
  skill vetting, optional `agent-browser` runtime adaptation, skill-lab
  evaluation, and UI polishing observer lanes
- first implementation pass registered in
  `zai-skill-stack-harvest-implementation-result.md`
- native surfaces now exist for external skill vetting, `agent-browser`
  adaptation, skill-evaluation lab behavior, and UI polishing observation
- post-implementation benchmark registered in
  `zai-skill-stack-post-implementation-benchmark-result.md`
- root arbitration now treats the specific GLM-harvest capability translation
  as `local ahead`, because the legacy lacks first-class workflows for the new
  vetting, adapter, evaluation, and polishing surfaces
- second independent replay registered in
  `web-reader-external-skill-replay-result.md`
- the replay exercised the new external skill vetting and skill-evaluation lab
  surfaces against the GLM / Z.ai `web-reader` skill, rejected direct import,
  preserved the useful source-observation pattern, and registered a bounded
  `pattern-only` insertion decision
- promotion-targeted rerun registered in
  `self-evolution-web-reader-replay-parity-result.md`
- both local and legacy/global judges promoted broad `autoresearch /
  self-evolution` to `local at parity` for the document-backed external
  workflow-learning loop
- this promotion excludes scheduled observers, browser/UI polishing runtime
  proof, blind-comparator output-quality proof, future `*.toml` agents, and any
  concrete web-reader-like runtime adapter
- focused maturity-control de-escalation rerun registered in
  `maturity-control-deescalation-rerun-result.md`
- both local and legacy/global judges kept `maturity control` at `near parity`
  and recommended no new doctrine; the mature behavior is de-escalating the
  benchmark loop

### 10. Parity / Replacement Gate

Target:

- satisfied; promoted beyond `near parity`

Current evidence status:

- focused parity-gate rerun registered in
  `parity-replacement-gate-rerun-result.md`
- both local and legacy/global judges promoted `parity / replacement gate` to
  `local at parity`, while rejecting `local ahead`
- repeated durable evidence now shows mixed behavior: the gate promotes
  evidence-backed surfaces and blocks wording-led, transient, or behavior-thin
  claims

## Tier 4: Surface-Specific Premium Discipline

### 11. Product-Critical Surface Discipline
Why it moved:

- the auth-recovery 2x2 rerun already cleared parity
- the later onboarding-critical visual/composition rerun pushed the surface
  beyond parity
- the surface is now promoted beyond `near parity`

### 12. Premium Interface Discipline

Why it moved:

- the premium billing recovery / upgrade rerun did not expose a local doctrine
  hole
- the local side produced the sharper premium failure-mode judgment
- the surface is now promoted beyond `near parity`

## Lower-Priority Structural Surfaces

These remain important, but are not the best immediate promotion targets:

- `subagent model`

Promoted beyond `near parity` during later benchmark work:

- `runtime packet schema`
- `QA / proof stack`
- `product-critical surface discipline`
- `premium interface discipline`
- `workflow catalog`
- `parity / replacement gate`
- `autoresearch / self-evolution`

Why:

- `runtime packet schema` was promoted after the native schema absorbed the
  detailed packet fields and the focused benchmark found local authority richer
  than the reference-backed surface
- `workflow catalog` was promoted after correction, explicit/light reruns, a
  full independent local-vs-legacy rerun, and a promotion-targeted legacy judge
  found no material remaining operational deficit
- `parity / replacement gate` was promoted after both local and legacy/global
  judges found the explicit local gate now matches the implicit legacy behavior
  for promotion and refusal decisions
- `autoresearch / self-evolution` was promoted after the Z.ai / GLM harvest,
  native implementation pass, independent `web-reader` replay, and matching
  local plus legacy/global promotion-targeted judges
- `subagent model` needs more real delegation-heavy runs than this repo is
  currently producing

## Immediate Operating Sequence

1. treat `maturity control` as evidence-narrowed but still `near parity`
2. monitor `autoresearch / self-evolution` in future external-learning sessions
   for over-promotion beyond its bounded document-backed scope
3. monitor `parity / replacement gate` in future live or benchmarked sessions
   for under-application, not for missing mechanism
4. use real or benchmarked runs to watch for regression on recently promoted
   calibration / issue-stack / workflow-catalog / parity-gate surfaces

## Success Condition

This queue is working if the next promotion cycle produces:

- durable behavioral evidence for the remaining `near parity` surfaces
- and the reason is repeated behavioral evidence, not doc-only argument
