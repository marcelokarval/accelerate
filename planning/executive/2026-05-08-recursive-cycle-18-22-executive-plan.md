# Recursive Cycle 18..22 Executive Plan

Date: 2026-05-08
Root: Accelerate recursive self-improvement
Repository: `/home/marcelo-karval/Backup/Projetos/accelerate`
Root orchestrator: Claw
Subagent budget: maximum 3 active delegated agents total

## Branch Entry Packet

- classification: orchestrated non-trivial work
- active branch: recursive self-improvement cycle 18..22
- active persona: root orchestrator + final forensic reviewer, not primary executor
- active stack: Linear live fixture proof, browser-proof server/capture and persistent regression separation, generated host skill export, bounded agent runtime binding candidate, semantic negative/status-surface governance
- active skills:
  - accelerate
  - subagent-governance
  - parallel-agents
  - executing-plans
  - verification-before-completion
- active ADRs / references:
  - `AGENTS.md`
  - `SKILL.md`
  - `README.md`
  - `.accelerate/status/readiness-dashboard.yaml`
  - `core/control-plane/recursive-improvement-situation-dashboard.md`
  - `core/control-plane/capability-maturity-dashboard.md`
  - `core/control-plane/runtime-adapter-maturity-dashboard.md`
  - `core/control-plane/skill-sync-topology.md`
  - `core/control-plane/agent-factory-promotion-pipeline.md`
  - `core/delegation/agent-factory-promotion-pipeline.md`
  - `core/runtime-packets/browser-proof-packet.md`
  - `adapters/runtime/browser/browser-truth-contract.md`
  - `planning/executive/2026-05-08-recursive-cycle-13-17-executive-plan.md`
  - `planning/executive/2026-05-08-recursive-cycle-13-17-task-ledger.md`
- local workspace:
  - `.accelerate=present`
  - action=reused + update active cycle pointers to 18..22
  - readiness dashboard=`.accelerate/status/readiness-dashboard.yaml`
  - current governing artifact=`planning/executive/2026-05-08-recursive-cycle-18-22-executive-plan.md`
  - current task ledger=`planning/executive/2026-05-08-recursive-cycle-18-22-task-ledger.md`
  - drift status=must be checked before closure
- gate ledger:
  - prompt-hardening=applied in this persisted plan
  - orchestrator-first-execution=active; root writes plan/ledger/final review and only blocker-class integration repairs
  - max-subagents=3
  - live-provider-safety=active for Linear; no secrets or non-fixture mutations in committed evidence
  - browser-server-monitoring=active; server-not-running and server-crashed-after-readiness must emit corrective packets
  - persistent-e2e-separation=active; browser capture cannot promote persistent regression
  - generated-host-boundary=active; user-home catalogs are not authority
  - agent-runtime-binding=bounded; no autonomous availability claim without actual runtime proof
  - semantic-negative-yaml-surfaces=active
  - final-verification=targeted tests + full suite + process cleanup + staged diff review + remote CI when pushed
- phase / SDLC: recursive improvement planning -> delegated bounded execution/review -> root final review-of-review
- issue stack status: planning docs remain governing artifact; do not invent remote issue authority
- QA / proof lane: targeted task gates, `bash tests/all.sh`, `git diff --check`, staged diff, process inspection, remote CI after push
- browser-proof intensity: high; active server monitoring and real local fixture capture/capture-failed diagnostics are in scope
- persistent E2E status: planned until a separate repo-owned regression proof lands
- local review / closure action: root final review and closure packet
- single-threaded exception: n/a; implementation/review delegated under max-3 budget

## Prompt Hardening Packet

- Prompt A: Proceed with the 1..5 next steps; build complete detailed executive plan and tasks; initiate execution and task reviews through subagents; root is orchestrator/final reviewer, maximum 3 subagents; actively close delivered idle agents, do not misclassify slow work as stuck, replace true stalls; analyze browser-proof because server frequently is not running correctly and capture must monitor output and emit correction; final report must include what was done and anticipated next steps.
- Prompt B: Execute Accelerate recursive cycle 18..22 as a bounded max-3-subagent orchestration. Persist plan and task ledger first. Delegate grouped slices for: (1) Linear live fixture readiness/proof path and provider-live semantic negatives; (2) browser-proof server monitoring, actual capture/capture-failed diagnostics, and separate persistent regression handoff; (3) generated host skill export follow-through, bounded agent runtime candidate proof, dashboards/dogfood/semantic YAML status surfaces, and next queue. Each subagent must anchor worktree, respect write scopes, run targeted proofs, self-review, and return a Subagent Return Packet. Root validates actual diffs and tests, fixes only blocker-class integration gaps if needed, reviews subagent review quality, checks process cleanup, commits/pushes when supported, watches CI, then reports done/residuals/next steps.
- Material changes: five residuals from RC13..17 are converted into five bounded tasks but staffed by at most three subagents through grouped non-conflicting scopes; browser-proof server monitoring receives explicit priority.

## Executive Objective

Turn the RC13..17 residual queue into proof-bearing, status-honest improvements without optimistic promotion. The cycle should improve live-readiness and runtime diagnostics, prove or keep blocked each capability, and leave the next queue based on updated evidence rather than copied old blockers.

## Success Criteria

1. Linear live fixture path can run a credential-safe preflight and, only when safe configuration exists, perform non-sensitive live fixture proof; otherwise it emits durable blocked evidence and semantic gates prevent provider-live promotion.
2. Browser-proof catches server-not-running, server-crashed-after-readiness, route/runtime capture failure, and fixture cleanup conditions; its packet output is syntactically valid on every failure path and redacts server/browser logs.
3. Persistent regression is separated from browser capture by a repo-owned handoff/proof stub or contract test that blocks any `available` claim without a separate proof locator.
4. Skill export generated-host proof remains available only inside temp/approved generated targets, with rollback/drift/cleanup proof and no user-home authority drift.
5. One bounded agent runtime candidate advances only where actual invocation/lifecycle/idle cleanup/demotion/root acceptance can be proven; otherwise it remains blocked with stronger criteria and negative gates.
6. Dashboards, `.accelerate` dogfood state, semantic negative fixtures, and YAML/status-surface checks reflect RC18..22 truth.
7. Final root review validates subagent packets against actual files, runs targeted gates and full suite, checks no owned fixture/background processes remain, reviews staged diff, commits/pushes, and watches remote CI if local proof supports closure.

## Non-Goals

- Do not commit provider secrets, private Linear payloads, non-fixture issue data, sensitive screenshots, or generated private workflow proof.
- Do not promote Linear live writes, persistent E2E, real host runtime skill installation, or autonomous agent runtime without durable proof locators.
- Do not write to real `~/.codex/skills`, `~/.claude/skills`, or `~/.agents/skills` during tests.
- Do not kill ambient Chrome/MCP/Playwright processes unless ownership or true trapped-idle state is confirmed.
- Do not start broad product work outside Accelerate control-plane/runtime proof surfaces.
- Do not mix unrelated repository changes or parallel user work.

## Execution Model And Monitoring

Root owns sequencing, plan/ledger, final review, final integration proof, process cleanup, commit/push/CI, and report. Subagents own bounded execution/review slices. Root may make direct code/doc edits only for plan/ledger/final-review artifacts or narrow blocker-class integration repairs discovered during final proof.

Active monitoring rules:

- Maximum 3 subagents total for this cycle.
- Prefer one wave of three grouped slices; do not spawn replacements unless a delegated call returns timeout/stall/partial evidence.
- A delivered Subagent Return Packet closes that agent; root does not keep it idle for unrelated follow-up.
- A task is not called stalled merely because it is long-running. Treat it as truly stalled only if the delegated interaction fails/returns no packet or tool status indicates interruption/timeout with no responsive progress.
- Timed-out partial artifacts are evidence, not delivery: root must inspect diffs and run proofs before accepting or replacing.
- Every subagent must report whether it started servers, browser sessions, or background processes.
- Root closure must inspect tool-managed processes and fixture-owned server/browser processes; kill only owned leaked fixtures.

## Staffing Plan

| Subagent | Tasks | Role | Write scope | Forbidden scope |
| --- | --- | --- | --- | --- |
| A | RC18 + provider-live semantic negatives | Linear workflow adapter implementer/reviewer | Linear helper scripts/tests, live preflight/proof appendix, workflow registry, Linear rows in dashboards/semantic fixtures | Browser capture helper internals, skill export internals, agent factory internals, secrets/provider payloads |
| B | RC19 + RC20 | Browser/runtime proof implementer/reviewer | `capture-browser-proof.sh`, browser tests/docs/packet contract, persistent regression handoff tests/docs/status rows | Linear provider writes, skill export internals, agent runtime internals |
| C | RC21 + RC22 + governance integration | Skill/agent/governance implementer/reviewer | skill export proof tests/docs if needed, agent factory docs/fixtures/tests, dashboards, `.accelerate` pointers, semantic YAML/status tests, next queue | Real user-home catalog writes, provider writes, ambient process cleanup outside owned fixtures |

## Task Set

### RC18 — Linear live fixture proof and provider-live status negatives

Goal: convert Linear live proof from generic planned residual into a repeatable, credential-safe proof path. If safe fixture config is absent, improve the blocked evidence and semantic guards rather than pretending availability.

Primary files:

- `onboarding/local-workspace/preflight-linear-mcp-live-fixture.sh`
- `onboarding/local-workspace/read-linear-mcp-adapter.sh`
- `onboarding/local-workspace/create-linear-mcp-issue.sh`
- `onboarding/local-workspace/attach-linear-mcp-artifact.sh`
- `onboarding/local-workspace/comment-linear-mcp-closure.sh`
- `onboarding/local-workspace/update-linear-mcp-status.sh`
- `tests/linear-structured-mcp-binding.sh`
- `adapters/workflow/remote-write-registry.yaml`
- `planning/evidence/dated-proof-appendix/linear-mcp-live-validation-2026-05-08.md`
- `core/control-plane/capability-maturity-dashboard.md`
- `core/control-plane/recursive-improvement-situation-dashboard.md`
- semantic negative fixtures/tests if provider-live status rows are covered

Required work:

- Validate preflight path redacts credentials and records only present/absent config.
- Add provider-live status negative coverage if dashboards/YAML could promote Linear without proof.
- Run live preflight. If `LINEAR_API_KEY`, `ACCELERATE_LINEAR_LIVE_FIXTURE=1`, fixture team, and fixture status exist, run the live fixture chain only against the explicit non-sensitive fixture. If not, update blocked appendix with exact missing prerequisites and no remote payloads.
- Keep status `planned` unless live proof locator exists.

Stop rules:

- Stop before provider mutation unless all explicit fixture gates are present.
- Stop before printing or committing tokens/provider response bodies/private data.
- Do not change non-fixture Linear issues.

Proof:

- `bash tests/linear-structured-mcp-binding.sh`
- blocked or live sanitized proof appendix

### RC19 — Browser-proof server monitoring and capture correction

Goal: fix and harden browser-proof paths so server failures and capture failures always emit valid JSON correction packets with monitored output. This directly addresses repeated captures attempted against bad or dead servers.

Primary files:

- `onboarding/local-workspace/capture-browser-proof.sh`
- `tests/browser-proof-monitoring.sh`
- `core/runtime-packets/browser-proof-packet.md`
- `adapters/runtime/browser/browser-truth-contract.md`
- `core/control-plane/runtime-adapter-maturity-dashboard.md`
- `.accelerate/status/readiness-dashboard.yaml`

Required work:

- Inspect and fix shell/Python/JS heredoc syntax so all packet-writing paths are executable, not only shell-parseable.
- Ensure server readiness checks run before browser launch and include HTTP code, tracked PID, liveness, stdout/stderr tails, and correction signal.
- Ensure server-crashed-after-readiness is distinguished from browser runtime unavailable.
- Ensure logs are redacted consistently in Python and JS paths.
- Ensure fixture servers/browser profiles are cleanup-trapped and leak-tested.
- Attempt local capture against fixture server; captured or capture-failed are both acceptable if packet is honest and actionable.

Stop rules:

- Localhost-only.
- No sensitive screenshots committed.
- No ambient browser/process killing without ownership.

Proof:

- `bash tests/browser-proof-monitoring.sh`
- `bash -n onboarding/local-workspace/capture-browser-proof.sh`
- focused packet JSON parse/probe if new paths are added

### RC20 — Persistent regression separation and E2E handoff proof

Goal: prevent browser-capture proof from being mistaken for persistent regression. Add or strengthen a repo-owned handoff/status test that keeps persistent E2E `planned` until a separate proof locator exists.

Primary files:

- `core/runtime-packets/browser-proof-packet.md`
- `adapters/runtime/browser/browser-truth-contract.md`
- `core/control-plane/runtime-adapter-maturity-dashboard.md`
- `.accelerate/status/readiness-dashboard.yaml`
- `tests/browser-proof-monitoring.sh`
- `tests/semantic-negative-fixtures.sh`

Required work:

- Add/strengthen contract language and tests for `persistent_regression_handoff.required_before_persistent_e2e_claim`.
- Add semantic negative coverage for YAML/status surfaces that might claim persistent regression availability from browser capture alone.
- Keep persistent regression unpromoted unless a separate test/proof is actually implemented.

Proof:

- `bash tests/browser-proof-monitoring.sh`
- `bash tests/semantic-negative-fixtures.sh`

### RC21 — Generated host skill export follow-through and bounded agent runtime candidate

Goal: preserve generated-host export truth and advance agent runtime candidate only through actual proofable lifecycle criteria. This grouped task should not broaden claims; it should strengthen proof/demotion paths.

Primary files:

- `scripts/export-skill-proof.sh`
- `tests/skill-export-proof.sh`
- `core/control-plane/skill-sync-topology.md`
- `skills/README.md`
- `skills/_registry/manifest.md`
- `core/control-plane/agent-factory-promotion-pipeline.md`
- `core/delegation/agent-factory-promotion-pipeline.md`
- `tests/promotion-replay-fixtures.sh`
- `tests/agent-install-export-contract.sh`
- `planning/evidence/dated-proof-appendix/skill-export-proof-2026-05-08.md`
- `planning/evidence/dated-proof-appendix/agent-factory-replay-2026-05-08.md`

Required work:

- Re-run/generated-host export proof and ensure refusal of user-home targets remains covered.
- Add any missing negative coverage for generated-host status surface promotion.
- For the bounded proof-auditor candidate, add actual runtime-binding proof only if a repo-owned adapter exists. If not, sharpen lifecycle/idle cleanup/demotion criteria and keep blocked.
- Add or improve tests so replay is not misread as autonomous runtime.

Proof:

- `bash tests/skill-export-proof.sh`
- `bash tests/promotion-replay-fixtures.sh`
- `bash tests/agent-install-export-contract.sh`

### RC22 — Governance integration, dogfood state, semantic YAML/status negatives, next queue

Goal: reconcile outcomes from RC18..RC21 into dashboards and dogfood state, and expand negative gates to packet/YAML surfaces named by the previous cycle: provider-live, generated-host, agent-runtime, and persistent-regression.

Primary files:

- `.accelerate/status/readiness-dashboard.yaml`
- `.accelerate/workflow/active-work-item.yaml`
- `.accelerate/review/handoff-summary.md` if present/generated by local workflow
- `core/control-plane/recursive-improvement-situation-dashboard.md`
- `core/control-plane/capability-maturity-dashboard.md`
- `core/control-plane/runtime-adapter-maturity-dashboard.md`
- `core/runtime-packets/recursive-improvement-cycle-packet.md`
- `tests/recursive-self-improvement-contract.sh`
- `tests/semantic-negative-fixtures.sh`
- `tests/dogfood-workspace-contract.sh`

Required work:

- Point dogfood active state to cycle 18..22 plan/ledger without committing private/generated outputs.
- Update dashboards only to statuses justified by RC18..RC21 proof.
- Add semantic negative fixtures/tests for YAML/status surface optimistic terms.
- Emit updated next queue from actual residuals.

Proof:

- `bash tests/control-plane-rc4-rc6.sh`
- `bash tests/recursive-self-improvement-contract.sh`
- `bash tests/semantic-negative-fixtures.sh`
- `bash tests/dogfood-workspace-contract.sh`

## Final Root Review Plan

Root final review must include:

1. `pwd && git status --short --branch` in `/home/marcelo-karval/Backup/Projetos/accelerate`.
2. Inspect subagent packets and discard any wrong-worktree or scope-violating output.
3. Inspect actual diff, not only summaries.
4. Run targeted gates from all tasks.
5. Run `bash tests/all.sh`.
6. Run `git diff --check`, stage all intended files, inspect `git diff --cached --stat`, and run `git diff --cached --check`.
7. Inspect `process list` and OS process probes for owned fixture leaks related to browser/server proof; kill only owned leaks.
8. Persist final review appendix.
9. Commit with existing repo message style if local proof supports closure.
10. Push and watch GitHub Actions for the final commit; report run URL/conclusion.

## Residual Policy

A blocked live provider or runtime proof can still be a successful cycle if the blocker is explicitly proven, dashboards remain honest, and semantic gates prevent unsupported promotion. Closure fails only if the repo claims availability without proof, tests fail, or owned processes/artifacts leak.
