# Maturity Control Post Subagent Rerun Result

## Purpose

Register the maturity-control rerun after the `subagent model` correction and
bounded promotion cycle.

The question was whether that cycle is now direct maturity-control evidence
sufficient to promote `maturity control` from `near parity` to
`local at parity`.

## Runtime Posture

- date: 2026-04-21
- repository phase: standalone pre-agents
- workflow backend: pre-adapter / no-backend
- issue stack posture: approved no-backend executive-artifact exception
- proof lane: read-only benchmark judges plus durable executive registration
- model: `gpt-5.4`
- effort: `high`
- local judge output: `/tmp/accelerate-maturity-post-subagent-local.out`
- legacy/global judge output:
  `/tmp/accelerate-maturity-post-subagent-legacy.out`

## Scenario

The previous maturity-control arbitration explicitly moved active convergence
focus away from another maturity-control promotion loop and toward a real
structural surface:

- `subagent model`

The local run then:

- found a real local subagent-family routing gap
- corrected the smallest owning surface, `core/delegation/subagent-model.md`
- reran focused local and legacy/global delegation-heavy judges
- promoted only bounded delegation governance
- excluded `local ahead`, provider-boundary ownership, scheduled/source
  observers, and future `*.toml` agent promotion
- avoided adding a new maturity-control doctrine rule

## Judge Results

| Judge | Verdict | Reason |
| --- | --- | --- |
| local-authority judge | `keep near parity` | the subagent cycle is direct maturity-control evidence, but the local ladder still requires repeated direct behavior before promotion |
| legacy/global judge | `promote local at parity` | the local now matches legacy maturity-control behavior for the bounded scenario: de-escalate promotion pressure, redirect to a real structural surface, correct the smallest home, promote only bounded evidence-backed scope, and block overclaim |

## Root Arbitration

Decision:

- keep `maturity control` at `near parity`

Why:

- the evidence is now direct, not merely adjacent
- however, the judges split on whether one direct post-subagent cycle is enough
  for promotion
- the local judge correctly identified the stricter standing rule: maturity
  control needs repeated direct behavior where it shrinks, stops, or relocates
  work
- the legacy/global promotion is valuable signal, but not enough to override a
  split verdict for a replacement-gate surface

Do not promote to:

- `local at parity`
- `local ahead`

## What Improved

The residual gap narrowed materially.

The local has now demonstrated one direct maturity-control behavior:

- it stopped forcing maturity-control promotion pressure
- it redirected attention to `subagent model`
- it corrected the smallest owning surface
- it avoided expanding `maturity-control.md`
- it bounded promotion and exclusions precisely

This is stronger than the earlier adjacent evidence from `parity / replacement
gate` and `autoresearch / self-evolution`.

## Doctrine Decision

Do not mutate:

- `core/workflows/maturity-control.md`

Do not add:

- another maturity-control rule
- another benchmark-specific lane

The mature behavior remains restraint.

## Next Validation

The next qualifying evidence should be another natural or benchmarked cycle
where maturity-control directly:

- stops a low-value loop
- shrinks ceremony after sufficient proof
- relocates durable learning to the smallest correct home
- blocks promotion or doctrine expansion despite a plausible pressure to do so

If that second direct cycle aligns with this one, promotion to bounded
`local at parity` becomes realistic.

## AI Review Report

### Self-Review

This packet records the stronger evidence without laundering a split verdict
into promotion.

### Self-Forensic Review

The overclaim risk is high because the legacy/global judge promoted. The safer
root arbitration is to treat that as positive signal while preserving the local
replacement ladder until direct behavior repeats.
