# Quality Stack Post-Restart Handoff — 2026-08-12

## State

- Status: `pending-post-restart`
- Governing issue: `CODEX-1`, which remains open
- Boundary: first fresh Codex process after the governed source and runtime
  mirror reconciliation
- Current session may prepare static contracts and exports, but cannot attest a
  fresh-process startup, prompt inventory, discovery, routing, or spawn result.
- Pre-restart deployment is complete: schema-3 runtime receipt is installed,
  static parity is green, and Plane PROGRESS comment
  `7d5b8b92-048d-4666-9f4a-193edf32ede9` passed provider readback.

## Fresh Codex Proof Sequence

1. Start a new Codex session without reusing this process.
2. Capture the effective prompt inventory and discovery output.
3. Confirm the default real root loads `accelerate`, specification lifecycle,
   and local TDD while specialist-only skills remain outside the root core.
4. Route one bounded assignment to each new logical specialist profile.
5. Spawn a no-history specialist with the governed assignment packet.
6. Verify the return contract, read-only containment, self-review,
   self-forensic review, and root-owned closure boundary.
7. Compare specialist output with a root-only control and record any catalog
   budget or routing regression.
8. Attach the resulting evidence to CODEX-1; keep CODEX-1 open if any lane is
   missing, stale, or contradictory.

## Required Evidence

- fresh Codex process identity and command;
- prompt inventory/discovery artifact;
- effective root versus specialist skill lists;
- spawn packet and returned packet;
- proof that configuration did not become a false isolation or physical-agent
  promotion claim;
- catalog-budget comparison;
- provider readback for the eventual Plane lifecycle update.

## Stop Rules

- Do not reuse current-process prompt state as fresh discovery evidence.
- Do not infer a successful spawn from static TOML, policy JSON, or templates.
- Do not close CODEX-1 before this handoff has a fresh-process receipt and the
  root has completed review-of-review.
