# CODEX-26 Phase 1 Exit Revalidation — Hardened Prompt B

## Accepted instruction

> esta autorizado explicitamente o Prompt B

## Objective

Revalidate the complete Phase-1 candidate and reach either `CLOSURE_READY` or
the first evidenced `NO-GO`. Green tests alone do not imply acceptance.

## Required sequence

Execute `TASK-001` through `TASK-011` from
`planning/executive/2026-09-02-codex-26-phase1-exit-revalidation-task-graph.md`.
Freeze the full five-root candidate denominator, run the full root suite,
execute the real OpenSpec lane twice, audit all proposal exit requirements,
obtain fresh independent tester and normative-review verdicts, perform root
review-of-review, and only then attempt one governed Plane PROGRESS comment if
the handshake is proven safe.

## Non-goals

No candidate correction, frozen-history rewrite, broad cleanup, global or
user-home install/sync, runtime projection, namespace migration, WebUI, deploy,
release, Phase-2 implementation, direct Plane HTTP, or Plane `Done` transition.

## Stop rules

Stop on authority failure or expiry, hash drift, root-suite failure, real
OpenSpec failure/skip/divergence, independent rejection, ambiguous Plane
result, new material P0, required task-owned code change, or scope expansion.
The result is `NO-GO`; there is no implicit correction loop.

## Human boundary

`TASK-011` presents the formal decision and stops. `TASK-012` requires a new,
distinct operator GO after that decision.
