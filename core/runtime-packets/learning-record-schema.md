# Learning Record Schema

Learning records are operational observations, not doctrine.

Required fields:

- `ts`
- `type`
- `key`
- `insight`
- `source`
- `confidence`
- `trusted`
- `evidence`

Allowed types: `pattern`, `pitfall`, `preference`, `architecture`, `tool`,
`operational`, `design-system`, `workflow`.
