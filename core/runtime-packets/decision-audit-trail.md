# Decision Audit Trail Packet

Required fields:

- `decision_id`
- `question_id`
- `class`
- `recommendation`
- `selected_option`
- `rationale`
- `rejected_alternatives`
- `affected_artifacts`
- `source`
- `timestamp`

One-way-door decisions must include `risk`, `approval`, and `required_reproof`.
