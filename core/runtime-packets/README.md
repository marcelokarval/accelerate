# Runtime Packets

This layer will hold the packet schemas and cadence rules that keep the
`accelerate` control plane visible during execution.

Current authority still lives in:

- `references/runtime-packet-templates.md`
- `references/runtime-observability-cadence.md`

