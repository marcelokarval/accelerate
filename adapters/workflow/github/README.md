# GitHub Adapter

This directory is reserved for the dedicated GitHub workflow adapter.

The intended role is architectural parity with the Linear adapter:

- same control-plane semantics
- same issue topology model
- same lifecycle/reporting expectations
- different operational backend

This adapter does not exist yet. Its presence here is deliberate scaffolding for
the next migration slices.

