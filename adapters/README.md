# Adapters

Adapters translate the platform's abstract workflow, runtime, and docs
capabilities into concrete backends and tools.

The important rule is:

- core law stays stable
- adapters change operational backend

This repository is still in the early shell phase. The adapter contracts are
being materialized here before the imported doctrine is fully rehomed.

