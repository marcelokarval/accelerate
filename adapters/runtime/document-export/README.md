# Document Export Runtime Adapter

The document export adapter renders plans, review packets, QA reports, or closure
packets into shareable HTML artifacts using
`onboarding/local-workspace/export-proof-document.sh`.

It keeps source markdown, output path, privacy class, renderer, and network asset
policy visible in `.accelerate/review/document-export-packet.md`.
