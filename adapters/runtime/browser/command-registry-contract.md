# Browser Command Registry Contract

A future browser runtime must use a command registry with read, write, and meta
command categories. Documentation, validation, and runtime dispatch should share
that registry to avoid drift.
