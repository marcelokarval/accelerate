interface:
  display_name: "Docker Compose Deployment Operations"
  short_description: "Deploy Compose services with safe rollback planning"
  default_prompt: "Use $docker-compose-deployment-operations to plan a safe Docker Compose deployment."
