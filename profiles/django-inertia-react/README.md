# Django Inertia React Profile

This is the current dominant default distribution inherited from the incubator.

It remains a first-class supported profile, but it should be treated as a stack
profile rather than as the universal definition of `accelerate`.

