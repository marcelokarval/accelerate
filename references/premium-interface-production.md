# Premium Interface Production Reference

## Authority

Primary native authority lives in:

- `../core/review/premium-interface-production.md`

This reference is intentionally a pointer. Premium UI doctrine was rehomed to `core/review/` and should not be maintained in parallel here because premium rules now interact with HTML design-system extraction and contract application.

## Use

Open the native file when deciding:

- whether a product-critical surface escalates to premium interface work
- whether reference / Figma / Stitch / design-system artifacts are sufficient
- whether a premium direction is more than a moodboard
- whether implementation must route through design-system contract application
- whether final UI is premium-aligned or technically correct but visually weak

If this file and the native file ever disagree, the native file wins.
