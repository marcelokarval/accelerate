# UI Mutation Ladder Reference

## Authority

Primary native authority lives in:

- `../core/control-plane/ui-mutation-ladder.md`

## Use

Open the native file when deciding:

- whether a UI change belongs to tokens, shared primitives, composites, shells,
  or pages
- whether premium work is being attempted too low in the ownership stack
- whether a greenfield system is starting from the right foundation order

If this file and the native file ever disagree, the native file wins.
