# OpenHands Harness

- Canonical ID: `prop4you.accelerate.harness.openhands`
- Projection mode: `generated-projection`
- Existing compatibility readers: `adapters/runtime/runtime-consumer-registry.json`, `adapters/runtime/cross-runtime-bootstrap-manifest.json`
- Authority: repository definitions; OpenHands owns profile discovery and native child execution.
- Current claim: export-only/binding-unavailable. Generated Markdown definitions are not proof of child callability or authorization.

`~/.agents` may discover an installed projection, but it is not an authoring
source and this entry does not install or symlink anything.
