# Paperclip Harness

- Canonical ID: `prop4you.accelerate.harness.paperclip`
- Projection mode: `api-projection`
- Authority: the official Paperclip API is the live Paperclip authority; the repository owns only this catalog definition.
- Current claim: defined only. API visibility or a configured client does not prove a permitted mutation.

Every API projection requires a scoped authorization and provider readback.
Direct database writes and copied-credential HTTP are prohibited fallbacks.
Paperclip task state does not supersede the canonical Plane work-item lifecycle.
