# OpenCode Harness

- Canonical ID: `prop4you.accelerate.harness.opencode`
- Projection mode: `native-direct`
- Existing compatibility readers: `adapters/runtime/runtime-consumer-registry.json`, `adapters/runtime/cross-runtime-bootstrap-manifest.json`
- Authority: repository definitions; OpenCode owns its native task lifecycle.
- Current claim: legacy-reference/static contract. Native direct is a projection mode, not a current callability assertion.

A fresh native loader and callability receipt is required before task dispatch.
