# Workflow Catalog Reference

## Authority

Primary native authority lives in:

- `../core/workflows/catalog.md`

This reference is intentionally a pointer. Named workflow lanes now live in `core/` so execution sequencing, packet expectations, and proof requirements do not drift across duplicate catalogs.

## Use

Open the native file when deciding or auditing:

- entry shaping
- issue bootstrap
- design artifact extraction
- design-system application / recomposition
- implementation handoff
- QA / browser / regression proof
- governance, benchmark, docs-sync, and closure workflows

If this file and the native file ever disagree, the native file wins.
