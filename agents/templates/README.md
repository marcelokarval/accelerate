# Agent Templates

Reserved for future governed agent templates, including `*.toml`-oriented
promotion targets once the standalone platform reaches that phase.

In the current `standalone pre-agents` phase, templates are not a live
delivery surface.

They should be understood as future promotion targets only after:

- doctrine is natively rehomed
- promotion policy is explicit
- authority boundaries are stable
- the repo actually chooses to adopt promoted runtime agents
