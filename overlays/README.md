# Overlays

Overlays capture project-specific deployment truth that should not leak into the
platform core.

Examples:

- local status taxonomies
- project-specific commands
- public docs generation rules
- domain-specific governance

