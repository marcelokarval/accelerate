# Review Boundary

This directory may contain committed review boundary documentation only.

Generated review packets, browser proof JSON, screenshots, provider responses, and private proof captures are intentionally ignored by `.accelerate/.gitignore` and must remain local/generated unless a future task explicitly sanitizes and promotes a non-secret fixture.

Current generated/private examples:

- `browser-proof.json`
- `browser-proof.png`
- `*.jsonl`
- provider response payloads
